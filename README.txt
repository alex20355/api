this api rest is a template that have a purpose of be use by every database possbile:
to do it ->
change define to be like your database

if you need a second database, then create new variable like the other.
